import React from 'react';
import ReactDOM from 'react-dom';
import App from './components/App';

// Main component is being rendererd to the root id element
ReactDOM.render(<App/>, document.getElementById('root'));